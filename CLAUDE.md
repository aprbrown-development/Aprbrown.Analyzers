# CLAUDE.md

Guidance for Claude Code when working in this repository.

DotNet.Analysers is a Roslyn analyzer + code fix package for .NET. See `README.md`
for the planned layout and build commands.

## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues in `aprbrown-development/DotNet.Analysers`,
managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each label string equal to its name. See
`docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` and `docs/adr/` at the repo root. See
`docs/agents/domain.md`.
